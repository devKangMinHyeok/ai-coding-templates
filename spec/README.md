# spec 폴더

여기에 **무엇을 만들지**가 들어갑니다.

- `TEMPLATE.md` — 스펙 양식 (수정하지 마세요)
- `spec.md` — `/interview` 스킬이 질답을 거쳐 만들어 줍니다 (당신의 프로젝트 정의)

코딩을 시작하기 전에 항상 `/interview`로 이 폴더의 `spec.md`부터 채웁니다.
막연한 아이디어를 **3~4시간 안에 끝낼 수 있는 범위로 좁히는** 것이 핵심입니다.
그다음 `/impl`로 구현에 들어갑니다.
