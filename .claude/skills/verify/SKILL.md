---
name: verify
description: 코드 변경 후 검증(type-check·lint·build)을 실행하고, 통과할 때까지 스스로 고친다. 기능을 추가/수정한 뒤 "다 됐다"고 말하기 전에 반드시 실행. 검증이 초록불이 되기 전에는 작업 완료라고 말하지 말 것.
---

# /verify — 통과할 때까지 스스로 돈다

## 핵심 규칙
**"다 됐어요"라고 말하려면 검증을 통과해야 한다.** 사람이 매번 끼어들어 확인해주지 않아도, AI가 스스로 검증을 돌리고 고친다.

## 절차

1. 실행:
   ```bash
   pnpm verify
   ```
   (= `type-check` → `lint` → `build` 순서. 기계가 채점할 수 있는 것들이다.)

2. **실패하면**:
   - 에러 메시지를 읽는다.
   - 원인을 고친다.
   - 다시 `pnpm verify`.
   - **통과(초록불)할 때까지 이 루프를 스스로 반복**한다. 사용자에게 떠넘기지 않는다.

3. **통과하면**: 그때 비로소 "검증 통과했습니다"라고 보고한다.

## 왜 이렇게 하나
빌드가 되는지, 타입이 맞는지, 린트를 지키는지는 **정답이 하나라 기계가 판별**할 수 있다(결정적 작업).
이런 건 AI 말을 믿지 말고 **자동 검사로 강제**한다. 그래서 이 저장소는 커밋 시점에도 같은 검사를 한 번 더 막아둔다(`.husky/pre-commit`).
