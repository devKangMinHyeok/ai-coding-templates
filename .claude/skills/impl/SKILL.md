---
name: impl
description: 확정된 spec/spec.md를 작은 단계로 나눠 실제로 구현한다. /interview로 스펙이 확정된 뒤 사용. 각 단계마다 검증(/verify)을 돌려 통과시킨다.
---

# /impl — 스펙을 작게 나눠 구현한다

## 전제
`spec/spec.md`가 있어야 한다. 없거나 비어 있으면 **먼저 `/interview`부터** 하라고 안내하고 멈춘다.

> 영감: GitHub spec-kit의 `/implement`, obra/superpowers의 `writing-plans`(2~5분 단위로 잘게 쪼개기).

## 절차
1. `spec/spec.md`를 읽는다.
2. "꼭 되어야 하는 기능"을 **작은 단계로 쪼갠다**(한 단계 = 작고 검증 가능한 변경).
3. 각 단계마다:
   - 최소한으로 구현한다(많이 만들지 않는다).
   - **`/verify`(= `pnpm verify`)를 돌려 초록불이 될 때까지 고친다.**
   - 초록불이면 다음 단계로.
4. 완성 기준(spec의 "오늘의 성공")을 충족하면 멈추고 보고한다.
5. 사용자에게 **"`/ship` 으로 배포하세요"** 안내.

## 원칙
- **작게, 자주 검증.** 큰 덩어리로 만들고 끝에 한 번 검증하지 않는다.
- 검증이 초록불이 되기 전에는 그 단계를 "끝났다"고 하지 않는다.
- spec 범위 밖의 욕심(스코프 크리프)은 `나중에`로 미루고 사용자에게 묻는다.
